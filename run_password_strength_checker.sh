#!/bin/bash

python3 password_strength_checker.py $@

# In a shell script, the $@ variable represents all of the positional parameters passed to the script.

# Note that the shebang line is only used on Unix-like systems (including Linux and macOS); 
# on Windows, the file extension is used to determine the interpreter to use.